# If not running interactively, don't load this file.
[[ $- = *i* ]] || return
#
# Load all files in ~/bashrc.d
for f in ~/.bashrc.d/*.sh; do
  source "$f"
done
#
# DO NOT ADD CONTENT TO THIS FILE!
#
# ADD NEW FILES TO ~/.bashrc.d INSTEAD!
#
